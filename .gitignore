# =======================================================
# ไฟล์ Environment (สำคัญมาก!)
# ไฟล์เหล่านี้เก็บข้อมูลสำคัญ เช่น API Keys, รหัสผ่าน
# ห้าม commit ขึ้น Git เด็ดขาด
# =======================================================
.env
.env.*
!.env.example

# =======================================================
# Logs
# ไฟล์ Log ต่างๆ ที่ถูกสร้างขึ้นอัตโนมัติ
# =======================================================
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# =======================================================
# Dependencies
# โฟลเดอร์ที่เก็บแพ็คเกจทั้งหมด (ขนาดใหญ่มากและติดตั้งใหม่ได้)
# =======================================================
node_modules

# =======================================================
# Build output
# โฟลเดอร์ผลลัพธ์จากการ build โปรเจกต์เพื่อนำไปใช้งานจริง
# =======================================================
dist
dist-ssr

# =======================================================
# Test & Coverage output
# ไฟล์ผลลัพธ์จากการรันเทส
# =======================================================
coverage
/cypress/videos/
/cypress/screenshots/

# =======================================================
# OS generated files
# ไฟล์ที่ถูกสร้างโดยระบบปฏิบัติการ (macOS)
# =======================================================
.DS_Store

# =======================================================
# Editor directories and files
# ไฟล์ตั้งค่าของโปรแกรมเขียนโค้ด (VSCode, Idea, etc.)
# =======================================================
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.tsbuildinfo
